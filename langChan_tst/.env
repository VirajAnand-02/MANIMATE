# .env - fill values below
GENAI_API_KEY=AIzaSyCLNY3aOiW70oyoYtFtwxFOBlauKAhvCwI

# System prompt for the Script LLM (should direct it to output JSON exactly)
SYSTEM_PROMPT_SCRIPT=You are a planner. Given a lecture topic, produce EXACT JSON with key "scenes": a list of objects with "seq" (int), optional "text" (narration), optional "anim" (animation description). Output only valid JSON. Keep scenes short: 6-20s each, and make the whole lecture no longer than ~120s unless asked.

# System prompt for the Manim LLM (per scene). The model must output a single Scene subclass named GeneratedScene_{seq}.
SYSTEM_PROMPT_MANIM=You are a developer that writes Manim Community Edition Python scenes. Given a scene description (JSON) produce only a single Python file content that defines a Scene subclass named GeneratedScene_{seq}. Do NOT include file I/O, do NOT call external network. Use only `from manim import *`. Keep total scene runtime roughly equal to the requested duration. Output only the Python code for the scene. Focus on the "anim" part of json to create animations. 

# Model Configuration
SCRIPT_MODEL=gemini-2.5-flash
MANIM_MODEL=gemini-2.5-flash

# TTS Configuration
TTS_PROVIDER=gemini
TTS_VOICE_NAME=Charon
TTS_MODEL=tts-1

USE_BATCH_MANIM=true

THINKING_BUDGET=8000
